{% extends 'base.html.twig' %}
{% block navbar %}{% endblock %}
{% block title %}Program index{% endblock %}

{% block body %}
    <div class="container-fluid">
        <div class="container">
            <div class="row">
                <div class="col col-xl-6 .col-sm-6	.col-md-6	.col-lg-6">
                    <div class="col">
                        <h1>Découvrez {{ program.title }} !</h1>
                        <h5>{{ program.category.name }}</h5>
                        <a href="{{ path('program_show', {program: program.slug}) }}">
                            {% if program.poster is not null %}
                                <img src="{{ program.poster }}" alt="{{ program.poster }}" style="width:100%"/>
                            {% else %}
                                <img src="https://underscoremusic.co.uk/site/wp-content/uploads/2014/05/no-poster.jpg"
                                     style="width:100%"/>
                            {% endif %}
                        </a>
                        <p>{{ program.summary }}</p>
                    </div>

                </div>
            </div>

        </div>
    </div>
{% endblock %}
