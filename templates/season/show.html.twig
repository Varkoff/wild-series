{% extends 'base.html.twig' %}

{% block title %}Season{% endblock %}

{% block body %}
    <div class="container">
        <h1>{{ season.program.title }} / {{ season.number }} - {{ season.year }}</h1>
        <p>{{ season.description }}</p>
        <h3>Liste des épisodes</h3>
        {% for episode in season.episodes %}
            <a href="{{ path('episode_show', {id: episode.id}) }}">
                <li>{{ episode.number }} | {{ episode.title }}</li>
            </a>
            <p>{{ episode.synopsis }}</p>
        {% endfor %}

        <a href="{{ path('season_index') }}">back to list</a>

        <a href="{{ path('season_edit', {'id': season.id}) }}">edit</a>

        {{ include('season/_delete_form.html.twig') }}
    </div>

{% endblock %}
